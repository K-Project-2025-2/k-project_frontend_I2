# Swagger에 추가해야 할 API 목록 (최종 정리)

현재 Swagger에 있는 API와 없는 API를 비교하여 정리했습니다.

## ✅ Swagger에 있는 API (연동 완료)

### 택시 관련
- ✅ `POST /api/taxi/rooms` - 방 생성
- ✅ `POST /api/taxi/rooms/join` - 방 참여
- ✅ `POST /api/taxi/rooms/leave` - 방 나가기
- ✅ `GET /api/taxi/rooms/{roomCode}/messages` - 메시지 조회
- ✅ `POST /api/taxi/rooms/{roomCode}/messages` - 메시지 전송

### 마이페이지 관련
- ✅ `GET /me` - 프로필 조회
- ✅ `PUT /me` - 프로필 수정
- ✅ `GET /me/deposit` - 보증금 상태 조회
- ✅ `POST /me/deposit` - 보증금 납부
- ✅ `GET /me/notifications` - 알림 설정 조회
- ✅ `PUT /me/notifications` - 알림 설정 수정
- ✅ `POST /me/report` - 이용자 신고

### 기타
- ✅ `POST /lost-and-found` - 분실물 신고

---

## ⚠️ Swagger에 없는 API (백엔드에 추가 요청 필요)

### 🔴 높음 (즉시 필요)

#### 택시 방 관련
1. **방 목록 조회**
   - **엔드포인트**: `GET /api/taxi/rooms`
   - **설명**: 현재 활성화된 모든 택시 방 목록을 조회합니다.
   - **인증**: 불필요
   - **Query 파라미터** (optional):
     - `status` - 방 상태 필터 (ACTIVE, CLOSED 등)
     - `page` - 페이지 번호 (기본 0)
     - `size` - 페이지 크기 (기본 20)
   - **응답 예시**:
   ```json
   {
     "rooms": [
       {
         "id": 1,
         "roomCode": "ABC123",
         "meetingPoint": "기흥역",
         "destination": "강남대학교",
         "meetingTime": "2024-01-01T10:00:00",
         "capacity": 4,
         "status": "ACTIVE",
         "memberCount": 2,
         "leaderId": 1
       }
     ],
     "total": 10,
     "page": 0,
     "size": 20
   }
   ```

2. **방 상세 조회**
   - **엔드포인트**: `GET /api/taxi/rooms/{roomCode}`
   - **설명**: 특정 방의 상세 정보를 조회합니다.
   - **인증**: 불필요
   - **파라미터**: `roomCode` (path parameter)
   - **응답 예시**:
   ```json
   {
     "id": 1,
     "roomCode": "ABC123",
     "meetingPoint": "기흥역",
     "destination": "강남대학교",
     "meetingTime": "2024-01-01T10:00:00",
     "capacity": 4,
     "status": "ACTIVE",
     "memberCount": 2,
     "leaderId": 1,
     "members": [
       {
         "userId": 1,
         "username": "홍길동",
         "email": "user@kangnam.ac.kr"
       }
     ]
   }
   ```

3. **방 종료**
   - **엔드포인트**: `POST /api/taxi/rooms/{roomCode}/close`
   - **설명**: 방장이 방을 종료합니다.
   - **인증**: 필요 (방장만 가능)
   - **파라미터**: `roomCode` (path parameter)
   - **응답 예시**:
   ```json
   {
     "message": "방이 종료되었습니다."
   }
   ```

#### 운행 관련
4. **운행 시작**
   - **엔드포인트**: `POST /api/taxi/rooms/{roomCode}/operation/start`
   - **설명**: 방장이 운행을 시작합니다.
   - **인증**: 필요 (방장만 가능)
   - **파라미터**: `roomCode` (path parameter)
   - **응답 예시**:
   ```json
   {
     "message": "운행이 시작되었습니다.",
     "operationId": 1
   }
   ```

5. **운행 수락**
   - **엔드포인트**: `POST /api/taxi/rooms/{roomCode}/operation/accept`
   - **설명**: 참여자가 운행을 수락합니다.
   - **인증**: 필요
   - **파라미터**: `roomCode` (path parameter)
   - **응답 예시**:
   ```json
   {
     "message": "운행을 수락했습니다.",
     "acceptedAt": "2024-01-01T10:00:00"
   }
   ```

6. **운행 상태 조회**
   - **엔드포인트**: `GET /api/taxi/rooms/{roomCode}/operation/status`
   - **설명**: 운행 수락 상태를 조회합니다.
   - **인증**: 필요
   - **파라미터**: `roomCode` (path parameter)
   - **응답 예시**:
   ```json
   {
     "roomCode": "ABC123",
     "operationStarted": true,
     "acceptedUsers": [1, 2, 3],
     "totalMembers": 4,
     "allAccepted": false
   }
   ```

7. **운행 종료**
   - **엔드포인트**: `POST /api/taxi/rooms/{roomCode}/operation/end`
   - **설명**: 방장이 운행을 종료합니다.
   - **인증**: 필요 (방장만 가능)
   - **파라미터**: `roomCode` (path parameter)
   - **응답 예시**:
   ```json
   {
     "message": "운행이 종료되었습니다."
   }
   ```

#### 정산 관련
8. **정산 생성**
   - **엔드포인트**: `POST /api/taxi/rooms/{roomCode}/split`
   - **설명**: 방장이 정산 금액을 입력합니다.
   - **인증**: 필요 (방장만 가능)
   - **파라미터**: `roomCode` (path parameter)
   - **요청 본문**:
   ```json
   {
     "totalAmount": 10000
   }
   ```
   - **응답 예시**:
   ```json
   {
     "splitId": 1,
     "roomCode": "ABC123",
     "totalAmount": 10000,
     "perPersonAmount": 2500,
     "members": [
       {
         "userId": 1,
         "username": "홍길동",
         "amount": 2500,
         "paid": false
       }
     ]
   }
   ```

9. **정산 정보 조회**
   - **엔드포인트**: `GET /api/taxi/rooms/{roomCode}/split`
   - **설명**: 정산 정보를 조회합니다.
   - **인증**: 불필요
   - **파라미터**: `roomCode` (path parameter)
   - **응답 예시**:
   ```json
   {
     "splitId": 1,
     "roomCode": "ABC123",
     "totalAmount": 10000,
     "perPersonAmount": 2500,
     "members": [
       {
         "userId": 1,
         "username": "홍길동",
         "amount": 2500,
         "paid": true
       }
     ]
   }
   ```

10. **송금 완료 체크**
    - **엔드포인트**: `POST /api/taxi/rooms/{roomCode}/split/confirm`
    - **설명**: 참여자가 송금 완료를 체크합니다.
    - **인증**: 필요
    - **파라미터**: `roomCode` (path parameter)
    - **응답 예시**:
    ```json
    {
      "message": "송금 완료가 확인되었습니다."
    }
    ```

---

### 🟡 중간 (곧 필요)

#### 보증금 관련
11. **보증금 반환**
    - **엔드포인트**: `POST /me/deposit/refund`
    - **설명**: 보증금을 반환합니다.
    - **인증**: 필요
    - **응답 예시**:
    ```json
    {
      "message": "보증금이 반환되었습니다.",
      "refundAmount": 5000
    }
    ```

#### 계좌 관련
12. **계좌 등록**
    - **엔드포인트**: `POST /me/account`
    - **설명**: 사용자의 계좌 정보를 등록합니다.
    - **인증**: 필요
    - **요청 본문**:
    ```json
    {
      "bank": "국민은행",
      "accountNumber": "1234567890"
    }
    ```
    - **응답 예시**:
    ```json
    {
      "message": "계좌가 등록되었습니다.",
      "accountId": 1
    }
    ```

13. **계좌 조회**
    - **엔드포인트**: `GET /me/account`
    - **설명**: 사용자의 계좌 정보를 조회합니다.
    - **인증**: 필요
    - **응답 예시**:
    ```json
    {
      "bank": "국민은행",
      "accountNumber": "1234567890",
      "accountId": 1
    }
    ```

14. **계좌 수정**
    - **엔드포인트**: `PUT /me/account`
    - **설명**: 사용자의 계좌 정보를 수정합니다.
    - **인증**: 필요
    - **요청 본문**:
    ```json
    {
      "bank": "신한은행",
      "accountNumber": "9876543210"
    }
    ```
    - **응답 예시**:
    ```json
    {
      "message": "계좌 정보가 수정되었습니다."
    }
    ```

15. **계좌 삭제**
    - **엔드포인트**: `DELETE /me/account`
    - **설명**: 사용자의 계좌 정보를 삭제합니다.
    - **인증**: 필요
    - **응답 예시**:
    ```json
    {
      "message": "계좌가 삭제되었습니다."
    }
    ```

#### 비밀번호 관련
16. **비밀번호 변경**
    - **엔드포인트**: `PUT /me/password`
    - **설명**: 사용자의 비밀번호를 변경합니다.
    - **인증**: 필요
    - **요청 본문**:
    ```json
    {
      "currentPassword": "oldpassword123",
      "newPassword": "newpassword123"
    }
    ```
    - **응답 예시**:
    ```json
    {
      "message": "비밀번호가 변경되었습니다."
    }
    ```

#### 고객센터 관련
17. **고객센터 문의 등록**
    - **엔드포인트**: `POST /me/inquiries`
    - **설명**: 고객센터에 문의를 등록합니다.
    - **인증**: 필요
    - **요청 본문**:
    ```json
    {
      "title": "문의 제목",
      "content": "문의 내용"
    }
    ```
    - **응답 예시**:
    ```json
    {
      "message": "문의가 접수되었습니다.",
      "inquiryId": 1
    }
    ```

18. **문의 내역 조회**
    - **엔드포인트**: `GET /me/inquiries`
    - **설명**: 내 문의 내역을 조회합니다.
    - **인증**: 필요
    - **Query 파라미터** (optional):
      - `page` - 페이지 번호 (기본 0)
      - `size` - 페이지 크기 (기본 20)
    - **응답 예시**:
    ```json
    {
      "inquiries": [
        {
          "inquiryId": 1,
          "title": "문의 제목",
          "content": "문의 내용",
          "status": "PENDING",
          "createdAt": "2024-01-01T10:00:00",
          "answeredAt": null
        }
      ],
      "total": 10,
      "page": 0,
      "size": 20
    }
    ```

19. **문의 상세 조회**
    - **엔드포인트**: `GET /me/inquiries/{inquiryId}`
    - **설명**: 문의 상세 정보를 조회합니다.
    - **인증**: 필요
    - **파라미터**: `inquiryId` (path parameter)
    - **응답 예시**:
    ```json
    {
      "inquiryId": 1,
      "title": "문의 제목",
      "content": "문의 내용",
      "status": "ANSWERED",
      "createdAt": "2024-01-01T10:00:00",
      "answer": {
        "content": "답변 내용",
        "answeredAt": "2024-01-02T10:00:00"
      }
    }
    ```

#### 보증금 내역 관련
20. **보증금 충전 내역 조회**
    - **엔드포인트**: `GET /me/deposit/history`
    - **설명**: 보증금 충전/사용 내역을 조회합니다.
    - **인증**: 필요
    - **Query 파라미터** (optional):
      - `page` - 페이지 번호 (기본 0)
      - `size` - 페이지 크기 (기본 20)
    - **응답 예시**:
    ```json
    {
      "history": [
        {
          "id": 1,
          "type": "DEPOSIT",
          "amount": 5000,
          "description": "보증금 충전",
          "createdAt": "2024-01-01T10:00:00"
        },
        {
          "id": 2,
          "type": "WITHDRAWAL",
          "amount": -2500,
          "description": "택시비 정산",
          "createdAt": "2024-01-02T10:00:00"
        }
      ],
      "total": 10,
      "page": 0,
      "size": 20
    }
    ```

---

## 🟢 낮음 (추후 필요)

현재는 위의 API들이 우선적으로 필요합니다.

---

## 총 20개 API 필요

### 우선순위별 요약

**🔴 높음 (즉시 필요) - 10개**
1. 방 목록 조회
2. 방 상세 조회
3. 방 종료
4. 운행 시작
5. 운행 수락
6. 운행 상태 조회
7. 운행 종료
8. 정산 생성
9. 정산 정보 조회
10. 송금 완료 체크

**🟡 중간 (곧 필요) - 10개**
11. 보증금 반환
12. 계좌 등록
13. 계좌 조회
14. 계좌 수정
15. 계좌 삭제
16. 비밀번호 변경
17. 고객센터 문의 등록
18. 문의 내역 조회
19. 문의 상세 조회
20. 보증금 충전 내역 조회

---

## 참고사항

- 모든 API는 Swagger 문서에 추가되어야 합니다.
- 인증이 필요한 API는 Bearer 토큰을 Authorization 헤더에 포함해야 합니다.
- 에러 응답은 일관된 형식으로 반환되어야 합니다 (예: `{ "message": "에러 메시지" }`).
- `roomCode`는 문자열 타입입니다 (숫자가 아님).

